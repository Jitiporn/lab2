import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color.fromARGB(255, 233, 194, 207),
      appBar: AppBar(
        title: Text("652021043"),
        backgroundColor: const Color.fromARGB(255, 192, 82, 119),
      ),
    );
  }
}
